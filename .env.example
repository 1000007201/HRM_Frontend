VITE_API_BASE_URL=http://localhost:3000

# Dev-only gate for /register-company while the backend's ENABLE_PUBLIC_SIGNUP=false.
# Must match the backend's REGISTRATION_SECRET.
VITE_REGISTRATION_SECRET=
